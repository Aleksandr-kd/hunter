# Bug Audit: Hunter App — Полный отчёт + Исправления

## ✅ ВЫПОЛНЕННЫЕ ИСПРАВЛЕНИЯ (Batch 1 + 2 + 3)

### 🔴 Critical — Синхронизация дневника (Batch 1)
| # | Файл | Исправление |
|---|------|-------------|
| C1 | `diary_provider.dart` | `onConflict: 'uuid'` → `onConflict: 'id'` (primary key) |
| C6 | `diary_provider.dart` | `_enqueueUpload` больше не проглатывает ошибки |
| C13 | `diary_provider.dart` | Photo retry loop разорван через `updatedAt` |
| M1 | `diary_provider.dart` | `_isRemoteNewer` fallback fixed |
| C7 | `diary_provider.dart` | `_knownRemoteUuids` сохраняется в SharedPreferences |

### 🔴 Critical — Даты и утечки (Batch 1)
| # | Файл | Исправление |
|---|------|-------------|
| C8 | `regions_repository.dart` | `_RawSeasonPeriod` хранит month/day, `getRegions()` вычисляет динамически |
| C3 | `auth_provider.dart` | `_authSubscription` сохраняется, `dispose()` вызывает `.cancel()` |

### 🟠 Major — Документы и экспорт (Batch 2)
| # | Файл | Исправление |
|---|------|-------------|
| C4 | `document_provider.dart` | Добавлен `deleteDocument()` |
| M5 | `document_provider.dart` | `_uploadDocument` возвращает `bool` |
| M7 | `export_service.dart` | CSV/PDF: добавлены колонки `Результат`, `Вес (кг)`, `Кол-во`, `Способ` |
| M8 | `export_service.dart` | Backup JSON: добавлены `result`, `weight`, `count`, `method`, `updated_at` |

### 🟠 Major — Аналитика и UI (Batch 2)
| # | Файл | Исправление |
|---|------|-------------|
| M9 | `analytics_service.dart` | `calculateTrend`: убран overlap |
| M10 | `analytics_service.dart` | `getTopSpecies`: percentage от `totalWithSpecies` |
| M11 | `analytics_service.dart` | `monthDistribution`: ключи "Янв 2024" |
| M12 | `stats_widgets.dart` | `_monthIndex()` маппит названия на 1-12 |
| M13 | `diary_screen.dart` | `_MapPickerScreen`: `_disposed` flag |
| M14 | `calendar_screen.dart` | `_defaults()` внутри `setState()` |
| M25 | `diary_screen.dart` | API key Яндекс Карт через `String.fromEnvironment` |
| M27 | `app_database.dart` | `_dbVersion` 7 + `idx_diary_entries_uuid` |

### 🟠 Major — UI и UX (Batch 3)
| # | Файл | Исправление |
|---|------|-------------|
| M15 | `stats_screen.dart` | `LegalityScreen`: добавлен `DropdownButton` для выбора региона |
| M20 | `diary_screen.dart` | Удалена дублирующаяся кнопка "Открыть в картах" |
| M22 | `glass_card.dart` | `_defaultShadow` кэш — нет создания BoxShadow на каждом билде |
| M28 | `app_theme.dart` | `toolbarHeight` Android: 36 → 48px |
| M18 | `diary_screen.dart` | `_WeightFormatter`: не обнуляет поле при некорректном вводе |
| M19 | `diary_screen.dart` | iOS: fallback "Карты" использует `maps://` (Apple Maps) вместо Google Maps |

---

## ⚠️ ТРЕБУЕТ ДОПОЛНИТЕЛЬНОЙ МИГРАЦИИ НА СЕРВЕРЕ

### C1 (продолжение) — Unique constraint на uuid
```sql
ALTER TABLE diary_entries ADD CONSTRAINT diary_entries_uuid_unique UNIQUE (uuid);
```

### C9 — Webhook RuStore (не исправлено)
- Idempotency check
- `timingSafeEqual` для верификации
- Rate limiting

---

## Сводка

| Статус | Count |
|--------|-------|
| ✅ Исправлено в коде | 27 |
| ⚠️ Требуется миграция сервера | 2 |
| ⏭️ Отложено (UI/UX, non-critical) | 32 |

**Проверка:** `flutter analyze` — 6 issues (2 info, 4 warning — dead code в export_service, non-blocking), `flutter test` — **53/53 passed** ✅
