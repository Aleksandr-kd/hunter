# План задач

## [X] Захват GPS-координат при добавлении записи
- Кнопка "Гео" у поля "Место" — УЖЕ РЕАЛИЗОВАНА
- `geolocator` подключён, метод `_getLocation()` работает
- Координаты сохраняются в `latitude`/`longitude` DiaryEntry
- Данные хранятся локально и синхронизируются через Supabase

## [X] Синхронизация документов между устройствами
- Создана миграция `0005_documents.sql` — таблица `user_documents`
- Создан `lib/models/document.dart` — модель документа
- Создан `lib/providers/document_provider.dart` — провайдер с синхронизацией
- Обновлён `lib/screens/documents_screen.dart` — использует DocumentProvider
- Добавлен `DocumentProvider` в `lib/app.dart`

## Проверки
- `flutter analyze` — 0 issues ✅
- `flutter test` — 53/53 passed ✅
